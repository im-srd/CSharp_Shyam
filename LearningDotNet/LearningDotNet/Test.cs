using System;

namespace LearningDotNet
{
    internal class Test
    {
        public static void Main(string[] args)
        {
            //    Console.WriteLine("Static Array Initialization:");
            //    int[] arr = { 1, 2, 3 }; // Correctly declare the array with '[]' and add a semicolon

            //    for (int i = 0; i < arr.Length; i++) // Use '<' instead of '>'
            //    {
            //        Console.WriteLine(arr[i]);
            //    }
            //    // ----------------------------------------------------------------------
            //    // Multidiemnsional Array
            //    int[,] md_arr = { { 1, 2, 3 }, { 4, 5, 6 }, { 7, 8, 9 } };
            //    Console.WriteLine(md_arr.GetLength(0));
            ////for(int a=0; a<)
            ///

            //long a = long.Parse(Console.ReadLine());
            //Console.WriteLine("a = "+a.GetType());
            Console.WriteLine("12" * 12);

        }
    }
}
